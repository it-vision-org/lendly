<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only: allows plain HTTP to local development hosts so the app can
  reach a backend running on the developer's machine (emulator/device are
  not on HTTPS during local dev). Not merged into release builds.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
